add LLM / Agent call integration into tool base

Add tool execution to orchestrator tasks so they can be distributed

sucess/failure checks

Tool/ML decorators - allows tools/ML to be used as decorators or wrappers

MCP duplex integration
